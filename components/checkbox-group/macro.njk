{#
  CheckboxGroup macro — a group component that manages a collection of checkboxes.

  HTML tag: <div role="group">
  CSS class: checkbox-group
  Accessibility:
    - role="group" groups the checkboxes for assistive tech.
    - params.label or params.labelledBy provides the group's
      accessible name. Prefer labelledBy when a visible heading or
      <legend> (via Fieldset) already names the group.

  Params:
    label        string   Accessible name (aria-label).
    labelledBy   string   Id(s) naming the group (preferred).
    describedBy  string   aria-describedby id list.
    required     boolean  Sets aria-required on the group.
    id           string   Element id.
    classes      string   Consumer-provided extra CSS classes.
    attributes   object   Extra HTML attributes as key/value pairs.

  Body content: checkbox/label pairs via caller().

  Usage:
    {% from "components/checkbox-group/macro.njk" import checkboxGroup %}
    {% from "components/checkbox-input/macro.njk" import checkboxInput %}
    {% from "components/label/macro.njk" import label %}

    {% call checkboxGroup({ label: "Conditions" }) %}
      {{ checkboxInput({ id: "c-asthma", name: "conditions", value: "asthma" }) }}
      {{ label({ for: "c-asthma", text: "Asthma" }) }}
    {% endcall %}
#}
{%- macro checkboxGroup(params) -%}
{%- set p = params | default({}) -%}
<div
  class="checkbox-group{% if p.classes %} {{ p.classes }}{% endif %}"
  role="group"
  {%- if p.labelledBy %} aria-labelledby="{{ p.labelledBy }}"
  {%- elif p.label %} aria-label="{{ p.label }}"{% endif %}
  {%- if p.describedBy %} aria-describedby="{{ p.describedBy }}"{% endif %}
  {%- if p.required %} aria-required="true"{% endif %}
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  {%- if p.html -%}{{ p.html | safe }}
  {%- elif caller -%}{{ caller() }}
  {%- endif -%}
</div>
{%- endmacro -%}
