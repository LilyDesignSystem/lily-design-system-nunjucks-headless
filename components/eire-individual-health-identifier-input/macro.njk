{#
  EireIndividualHealthIdentifierInput macro — an input for
    entering an Eire Individual Health Identifier (IHI).

  HTML tag: <input type="text">
  CSS class: eire-individual-health-identifier-input
  Format: seven digits, conventionally grouped "XXXX XXX".
  Accessibility: inputmode="numeric", autocomplete="off",
    spellcheck suppressed.

  Params:
    id, name, value, label, describedBy, errormessage, invalid,
    required, disabled, readonly, classes, attributes.

  Usage:
    {% from "components/eire-individual-health-identifier-input/macro.njk" import eireIndividualHealthIdentifierInput %}
    {{ eireIndividualHealthIdentifierInput({ id: "ihi" }) }}
#}
{%- macro eireIndividualHealthIdentifierInput(params) -%}
<input
  class="eire-individual-health-identifier-input{% if params.classes %} {{ params.classes }}{% endif %}"
  type="text"
  id="{{ params.id }}"
  name="{{ params.name | default(params.id) }}"
  value="{{ params.value | default('') }}"
  inputmode="numeric"
  pattern="[0-9 \-]{7,9}"
  maxlength="9"
  autocomplete="off"
  spellcheck="false"
  {%- if params.label %} aria-label="{{ params.label }}"{% endif %}
  {%- if params.describedBy %} aria-describedby="{{ params.describedBy }}"{% endif %}
  {%- if params.errormessage %} aria-errormessage="{{ params.errormessage }}"{% endif %}
  {%- if params.invalid %} aria-invalid="true"{% endif %}
  {%- if params.required %} required{% endif %}
  {%- if params.disabled %} disabled{% endif %}
  {%- if params.readonly %} readonly{% endif %}
  {%- if params.attributes %}{% for k, v in params.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
{%- endmacro -%}
