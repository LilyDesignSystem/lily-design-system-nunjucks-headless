{#
  EmailInput macro — an input for entering an email address.

  HTML tag: <input type="email">
  CSS class: email-input
  Accessibility: see TextInput for aria-describedby, aria-errormessage,
    and aria-invalid conventions.

  Params (same shared set as TextInput):
    id, name, value, label, describedBy, errormessage, invalid,
    required, disabled, readonly, placeholder, autocomplete,
    pattern, minlength, maxlength, multiple, classes, attributes.

  Usage:
    {% from "components/email-input/macro.njk" import emailInput %}
    {{ emailInput({ id: "email", autocomplete: "email", required: true }) }}
#}
{%- macro emailInput(params) -%}
<input
  class="email-input{% if params.classes %} {{ params.classes }}{% endif %}"
  type="email"
  id="{{ params.id }}"
  name="{{ params.name | default(params.id) }}"
  value="{{ params.value | default('') }}"
  {%- if params.label %} aria-label="{{ params.label }}"{% endif %}
  {%- if params.describedBy %} aria-describedby="{{ params.describedBy }}"{% endif %}
  {%- if params.errormessage %} aria-errormessage="{{ params.errormessage }}"{% endif %}
  {%- if params.invalid %} aria-invalid="true"{% endif %}
  {%- if params.required %} required{% endif %}
  {%- if params.disabled %} disabled{% endif %}
  {%- if params.readonly %} readonly{% endif %}
  {%- if params.placeholder %} placeholder="{{ params.placeholder }}"{% endif %}
  {%- if params.autocomplete %} autocomplete="{{ params.autocomplete }}"{% endif %}
  {%- if params.pattern %} pattern="{{ params.pattern }}"{% endif %}
  {%- if params.minlength %} minlength="{{ params.minlength }}"{% endif %}
  {%- if params.maxlength %} maxlength="{{ params.maxlength }}"{% endif %}
  {%- if params.multiple %} multiple{% endif %}
  {%- if params.attributes %}{% for k, v in params.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
{%- endmacro -%}
