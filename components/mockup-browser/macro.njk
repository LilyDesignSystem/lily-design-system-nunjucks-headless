{#
  MockupBrowser macro — a box area that looks like a web browser.

  HTML tag: <figure>
  CSS class: mockup-browser
  Accessibility: decorative chrome is aria-hidden; the body content
    is the meaningful part.

  Params:
    label       string   Optional aria-label on the figure.
    title       string   Optional title text in the chrome bar.
    id          string
    classes     string
    attributes  object

  Body content: the rendered UI preview, via caller().
#}
{%- macro mockupBrowser(params) -%}
{%- set p = params | default({}) -%}
<figure
  class="mockup-browser{% if p.classes %} {{ p.classes }}{% endif %}"
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.label %} aria-label="{{ p.label }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  <div class="mockup-browser-chrome" aria-hidden="true">
    {%- if p.title %}<span class="mockup-browser-title">{{ p.title }}</span>{% endif %}
  </div>
  <div class="mockup-browser-body">
    {%- if p.html -%}{{ p.html | safe }}
    {%- elif caller -%}{{ caller() }}
    {%- endif -%}
  </div>
</figure>
{%- endmacro -%}
