{#
  MockupShell macro — a box area that looks like a terminal shell.

  HTML tag: <figure>
  CSS class: mockup-shell
  Accessibility: decorative chrome is aria-hidden; the body content
    is the meaningful part.

  Params:
    label       string   Optional aria-label on the figure.
    title       string   Optional title text in the chrome bar.
    id          string
    classes     string
    attributes  object

  Body content: the rendered UI preview, via caller().
#}
{%- macro mockupShell(params) -%}
{%- set p = params | default({}) -%}
<figure
  class="mockup-shell{% if p.classes %} {{ p.classes }}{% endif %}"
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.label %} aria-label="{{ p.label }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  <div class="mockup-shell-chrome" aria-hidden="true">
    {%- if p.title %}<span class="mockup-shell-title">{{ p.title }}</span>{% endif %}
  </div>
  <div class="mockup-shell-body">
    {%- if p.html -%}{{ p.html | safe }}
    {%- elif caller -%}{{ caller() }}
    {%- endif -%}
  </div>
</figure>
{%- endmacro -%}
