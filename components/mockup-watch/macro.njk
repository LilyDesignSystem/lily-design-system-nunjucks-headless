{#
  MockupWatch macro — a box area that looks like a smart watch.

  HTML tag: <figure>
  CSS class: mockup-watch
  Accessibility: decorative chrome is aria-hidden; the body content
    is the meaningful part.

  Params:
    label       string   Optional aria-label on the figure.
    title       string   Optional title text in the chrome bar.
    id          string
    classes     string
    attributes  object

  Body content: the rendered UI preview, via caller().
#}
{%- macro mockupWatch(params) -%}
{%- set p = params | default({}) -%}
<figure
  class="mockup-watch{% if p.classes %} {{ p.classes }}{% endif %}"
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.label %} aria-label="{{ p.label }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  <div class="mockup-watch-chrome" aria-hidden="true">
    {%- if p.title %}<span class="mockup-watch-title">{{ p.title }}</span>{% endif %}
  </div>
  <div class="mockup-watch-body">
    {%- if p.html -%}{{ p.html | safe }}
    {%- elif caller -%}{{ caller() }}
    {%- endif -%}
  </div>
</figure>
{%- endmacro -%}
