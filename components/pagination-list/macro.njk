{#
  PaginationList macro — an ordered list of page navigation links.

  HTML tag: <ol>
  CSS class: pagination-list

  Params:
    id          string   Element id.
    classes     string   Consumer-provided extra CSS classes.
    attributes  object   Extra HTML attributes as key/value pairs.

  Body content: PaginationListItem children via caller().

  Usage: see PaginationNav.
#}
{%- macro paginationList(params) -%}
{%- set p = params | default({}) -%}
<ol
  class="pagination-list{% if p.classes %} {{ p.classes }}{% endif %}"
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  {%- if p.html -%}{{ p.html | safe }}
  {%- elif caller -%}{{ caller() }}
  {%- endif -%}
</ol>
{%- endmacro -%}
