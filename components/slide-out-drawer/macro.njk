{#
  SlideOutDrawer macro — dialog overlay.

  HTML tag: <div role="dialog">
  CSS class: slide-out-drawer

  Params: label (required), open (default false), id, classes, attributes. Caller provides content.
#}
{%- macro slideOutDrawer(params) -%}
{%- set p = params | default({}) -%}
<div
  class="slide-out-drawer{% if p.classes %} {{ p.classes }}{% endif %}"
  role="dialog" aria-modal="false"
  aria-label="{{ p.label | default('Drawer') }}"
  {%- if not p.open %} hidden{% endif %}
  {%- if p.id %} id="{{ p.id }}"{% endif %}
  {%- if p.attributes %}{% for k, v in p.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
  {%- if p.html -%}{{ p.html | safe }}
  {%- elif caller -%}{{ caller() }}
  {%- endif -%}
</div>
{%- endmacro -%}
