{#
  TextAreaInput macro — a multi-line text input area.

  HTML tag: <textarea>
  CSS class: text-area-input
  Accessibility: see TextInput for aria-describedby, aria-errormessage,
    and aria-invalid conventions.

  Params:
    id            string   Element id. Required.
    name          string   Name attribute. Defaults to id.
    value         string   Current value, rendered as element content.
    label         string   Accessible name (aria-label). Use only
                           when no <label for="..."> is associated.
    describedBy   string   aria-describedby id list.
    errormessage  string   aria-errormessage id.
    invalid       boolean  Sets aria-invalid="true" when truthy.
    required      boolean
    disabled      boolean
    readonly      boolean
    placeholder   string
    autocomplete  string
    rows          number   Rows attribute. Default 5.
    cols          number
    minlength     number
    maxlength     number
    spellcheck    boolean
    wrap          string   "hard" | "soft".
    classes       string   Consumer-provided extra CSS classes.
    attributes    object   Extra HTML attributes as key/value pairs.

  Usage:
    {% from "components/text-area-input/macro.njk" import text-area-input %}
    {{ text-area-input({ id: "feedback", rows: 6 }) }}
#}
{%- macro text-area-input(params) -%}
<textarea
  class="text-area-input{% if params.classes %} {{ params.classes }}{% endif %}"
  id="{{ params.id }}"
  name="{{ params.name | default(params.id) }}"
  rows="{{ params.rows | default(5) }}"
  {%- if params.cols %} cols="{{ params.cols }}"{% endif %}
  {%- if params.label %} aria-label="{{ params.label }}"{% endif %}
  {%- if params.describedBy %} aria-describedby="{{ params.describedBy }}"{% endif %}
  {%- if params.errormessage %} aria-errormessage="{{ params.errormessage }}"{% endif %}
  {%- if params.invalid %} aria-invalid="true"{% endif %}
  {%- if params.required %} required{% endif %}
  {%- if params.disabled %} disabled{% endif %}
  {%- if params.readonly %} readonly{% endif %}
  {%- if params.placeholder %} placeholder="{{ params.placeholder }}"{% endif %}
  {%- if params.autocomplete %} autocomplete="{{ params.autocomplete }}"{% endif %}
  {%- if params.minlength %} minlength="{{ params.minlength }}"{% endif %}
  {%- if params.maxlength %} maxlength="{{ params.maxlength }}"{% endif %}
  {%- if params.wrap %} wrap="{{ params.wrap }}"{% endif %}
  {%- if params.spellcheck is defined %} spellcheck="{{ params.spellcheck }}"{% endif %}
  {%- if params.attributes %}{% for k, v in params.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>{{ params.value | default('') }}</textarea>
{%- endmacro -%}
