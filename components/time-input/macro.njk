{#
  TimeInput macro — an input for entering a time value.

  HTML tag: <input type="time">
  CSS class: time-input
  Accessibility: browser-native time control. See TextInput.

  Params:
    id, name, value (HH:MM or HH:MM:SS), min, max, step (seconds),
    label, describedBy, errormessage, invalid, required, disabled,
    readonly, autocomplete, classes, attributes.

  Usage:
    {% from "components/time-input/macro.njk" import timeInput %}
    {{ timeInput({ id: "appt-time", min: "09:00", max: "17:00" }) }}
#}
{%- macro timeInput(params) -%}
<input
  class="time-input{% if params.classes %} {{ params.classes }}{% endif %}"
  type="time"
  id="{{ params.id }}"
  name="{{ params.name | default(params.id) }}"
  value="{{ params.value | default('') }}"
  {%- if params.min %} min="{{ params.min }}"{% endif %}
  {%- if params.max %} max="{{ params.max }}"{% endif %}
  {%- if params.step %} step="{{ params.step }}"{% endif %}
  {%- if params.label %} aria-label="{{ params.label }}"{% endif %}
  {%- if params.describedBy %} aria-describedby="{{ params.describedBy }}"{% endif %}
  {%- if params.errormessage %} aria-errormessage="{{ params.errormessage }}"{% endif %}
  {%- if params.invalid %} aria-invalid="true"{% endif %}
  {%- if params.required %} required{% endif %}
  {%- if params.disabled %} disabled{% endif %}
  {%- if params.readonly %} readonly{% endif %}
  {%- if params.autocomplete %} autocomplete="{{ params.autocomplete }}"{% endif %}
  {%- if params.attributes %}{% for k, v in params.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
{%- endmacro -%}
