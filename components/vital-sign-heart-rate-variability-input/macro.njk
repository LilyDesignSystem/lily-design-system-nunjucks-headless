{#
  VitalSignHeartRateVariabilityInput macro — a number input for
    heart rate variability (HRV), typically RMSSD in milliseconds.

  HTML tag: <input type="number">
  CSS class: vital-sign-heart-rate-variability-input
  Clinical range: defaults to 1–250 ms. Integer step.

  Params:
    id, name, value, label, describedBy, errormessage, invalid,
    required, disabled, readonly, min (1), max (250), step (1),
    classes, attributes.
#}
{%- macro vitalSignHeartRateVariabilityInput(params) -%}
<input
  class="vital-sign-heart-rate-variability-input{% if params.classes %} {{ params.classes }}{% endif %}"
  type="number"
  id="{{ params.id }}"
  name="{{ params.name | default(params.id) }}"
  value="{{ params.value | default('') }}"
  min="{{ params.min | default(1) }}"
  max="{{ params.max | default(250) }}"
  step="{{ params.step | default(1) }}"
  inputmode="numeric"
  {%- if params.label %} aria-label="{{ params.label }}"{% endif %}
  {%- if params.describedBy %} aria-describedby="{{ params.describedBy }}"{% endif %}
  {%- if params.errormessage %} aria-errormessage="{{ params.errormessage }}"{% endif %}
  {%- if params.invalid %} aria-invalid="true"{% endif %}
  {%- if params.required %} required{% endif %}
  {%- if params.disabled %} disabled{% endif %}
  {%- if params.readonly %} readonly{% endif %}
  {%- if params.attributes %}{% for k, v in params.attributes %} {{ k }}="{{ v }}"{% endfor %}{% endif %}
>
{%- endmacro -%}
